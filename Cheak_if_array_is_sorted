public class Solution {
    public static int isSorted(int n, int []nums) {
                 for (int i = 1; i < nums.length; i++) {
            if ( nums[i] < nums[i-1]) {
                return 0;
            } 
                 }
            return 1;
    }
}
