public class Solution {
    static boolean isKthBitSet(int n, int k) {
        // Write your code here.
        int mask = 1 << (k-1);
        if((n & mask) != 0){
        return true;
        }
         return false;
    }
}
