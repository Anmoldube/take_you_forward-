class Solution {
    public int findMin(int[] nums) {
        int sum = nums[0];
        for(int i = 0 ; i < nums.length ; i++){
         sum = Math.min(sum,nums[i]);
       }
       return sum;
    }
}
