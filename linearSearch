import java.util.*;
public class Solution {
    public static int linearSearch(int n, int targt, int []arr){
        // Write your code here.
        for(int i = 0 ;i < arr.length;i++){
            if(arr[i] == targt){
                return i;
            }
        }
        return -1;

    }
}
